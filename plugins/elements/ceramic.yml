plugin:
    name: Elements

    runtime:
        if plugin_elements:
            +libs:
                - fuzzaldrin: 0.1.1
            +paths:
                - {plugin:cwd}/runtime/src
            +assets:
                - {plugin:cwd}/assets
