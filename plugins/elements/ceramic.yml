plugin:
    name: Elements

    runtime:
        if plugin_elements:
            +libs:
                - fuzzaldrin
            +paths:
                - {plugin:cwd}/runtime/src
            +assets:
                - {plugin:cwd}/assets
