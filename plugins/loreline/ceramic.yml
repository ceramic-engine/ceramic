plugin:
    name: Loreline

    runtime:
        if plugin_loreline:
            +generated:
                - '{plugin:cwd}/tpl/generated'
            +libs:
                - loreline
            +paths:
                - '{plugin:cwd}/runtime/src'
