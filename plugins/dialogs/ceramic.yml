plugin:
    name: Dialogs
    tools: tools.DialogsPlugin

    runtime:
        if plugin_dialogs:
            if mac || linux || windows || ios || android:
                +libs:
                    - linc_dialogs