package backend;

#if !no_backend_docs
/**
 * Options for loading binary resources in the headless backend.
 * 
 * These options would typically control how binary files are loaded
 * and processed. In headless mode, these options are maintained for
 * API compatibility but don't affect actual binary loading behavior.
 * 
 * Currently, no specific options are defined, but the structure
 * allows for future expansion.
 */
#end
typedef LoadBinaryOptions = {
    
}
