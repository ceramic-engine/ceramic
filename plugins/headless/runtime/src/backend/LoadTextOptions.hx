package backend;

/**
 * Options for loading text resources in the headless backend.
 * 
 * These options would typically control how text files are loaded,
 * parsed, and processed. In headless mode, these options are
 * maintained for API compatibility but don't affect actual
 * text loading behavior.
 * 
 * Currently, no specific options are defined, but the structure
 * allows for future expansion.
 */
typedef LoadTextOptions = {
    
}
