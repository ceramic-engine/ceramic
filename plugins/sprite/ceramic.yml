plugin:
    name: Sprite

    runtime:
        if plugin_sprite:
            +libs:
                - ase
            +generated:
                - {plugin:cwd}/tpl/generated
            +paths:
                - {plugin:cwd}/runtime/src
